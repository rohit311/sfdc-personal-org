<aura:component implements="force:hasRecordId" access="global">
  <aura:handler name="init" value="{!this}" action="{!c.doInit}"/>

  <div>
    Record Id in child : {!v.recordId}
  </div>
</aura:component>
